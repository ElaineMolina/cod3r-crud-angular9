import { BluevioletDirective } from './blueviolet.directive';

describe('BluevioletDirective', () => {
  it('should create an instance', () => {
    const directive = new BluevioletDirective();
    expect(directive).toBeTruthy();
  });
});
